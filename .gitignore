target/
node_modules/
dist/
.hotline-dev/
.brainfile/
.claude/
crates/hotline-app/gen/
.hallmark/

# Private updater signing backup; never a release asset. The Toad-era spelling
# stays ignored too, so a checkout that still holds one cannot commit the key.
.hotline-release-signing/
.toad-release-signing/
__pycache__/

# wrangler state from deploying site/ and docs-site/
.wrangler/
